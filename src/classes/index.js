// Archivo para agrupar todas las clases que tengamos en la carpeta classes y sean exportadas

import { Todo } from './todo.class';
import { TodoList } from './todo-list.class';

// Exportamos las clases en un mismo archivo
export {
    Todo,
    TodoList
}